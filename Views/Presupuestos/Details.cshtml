@model Presupuestos

<h2>@Model.NombreDestinatario</h2>

<table class="table">
    <thead>
        <tr>
            <th>Productos</th>
            @* <th>Nombre del Destinatario</th>
            <th>Fecha de creacion</th> *@
            @* <th>Acciones</th> *@
        </tr>
    </thead>
    <tbody>
        @foreach (PresupuestosDetalle item in Model.Detalle)
        {
            <tr>
                <td>@item.productos.Description</td>
                @* <td></td>
                <td></td>
                <td>
                    <a asp-controller="Producto" asp-action="Edit" asp-route-id="@item.Id" class="btn btn-warning">Modificar</a>
                    <a asp-controller ="Producto" asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger">Eliminar</a>
                </td> *@
            </tr>
        }
    </tbody>
</table>